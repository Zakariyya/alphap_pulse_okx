[project]
name = "alphapulse-okx"
version = "0.1.0"
description = "OKX listing, funding/OI, and heat radar monitors."
requires-python = ">=3.10"
dependencies = [
  "requests>=2.32.0",
  "httpx>=0.28.0",
]

[dependency-groups]
dev = [
  "pytest>=8.0.0",
]
